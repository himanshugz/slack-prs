name: 'Slack Test'
description: 'Send PR notification to slack'
inputs:
  webhook_url:  # id of input
    description: 'Slack URL'
    required: true
    default: 'World'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'index.js'